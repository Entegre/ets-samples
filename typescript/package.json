{
  "name": "entegre-ets-typescript",
  "version": "1.0.0",
  "description": "Entegre ETS API Client - TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "live-test": "ts-node src/live-test.ts"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
